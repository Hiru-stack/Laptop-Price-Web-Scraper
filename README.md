# Laptop-Price-Web-Scraper
I used Python requests library to access a website, then used BeautifulSoup to parse the HTML structure and extract laptop details such as titles and prices. After collecting the data, I stored it using pandas and exported it as a CSV file
